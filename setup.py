#!/usr/bin/env python3

from setuptools import setup, find_packages

setup(
    name='wlcomp',
    version="0.1",
    packages=find_packages(),
    description="Python CFFI port for LComp library",
)
